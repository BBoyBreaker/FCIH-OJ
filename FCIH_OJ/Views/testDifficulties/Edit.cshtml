@model FCIH_OJ.Models.contestAndProblem.problemDifficulty

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>problemDifficulty</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.difficultyLetter)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.difficultyLetter)
            @Html.ValidationMessageFor(model => model.difficultyLetter)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
